import ready from '../utilities/_helper';
import renderGrid from '../components/_grid';
import renderDocuments from '../components/_documents';

ready(() => {
  renderGrid();
  renderDocuments();
});
